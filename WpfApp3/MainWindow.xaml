<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Matrix Multiplier" Height="600" Width="800">
    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
  
            <RowDefinition Height="Auto" />
          
            <RowDefinition Height="*" />
 
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Center">
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Матрица А" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock VerticalAlignment="Center" Margin="0,0,4,0">Строки:</TextBlock>
                    <TextBox x:Name="RowsA" Width="50" Text="2"/>
                    <TextBlock VerticalAlignment="Center" Margin="8,0,4,0">Столбцы:</TextBlock>
                    <TextBox x:Name="ColsA" Width="50" Text="2"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock Text="Матрица B" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock VerticalAlignment="Center" Margin="0,0,4,0">Строки:</TextBlock>
                    <TextBox x:Name="RowsB" Width="50" Text="2"/>
                    <TextBlock VerticalAlignment="Center" Margin="8,0,4,0">Столбцы:</TextBlock>
                    <TextBox x:Name="ColsB" Width="50" Text="2"/>
                </StackPanel>
            </StackPanel>

            <Button Content="Создать матрицы" Click="Generate_Click" Margin="10"/>
            <Button Content="Умножить" Click="Multiply_Click" Margin="10"/>
        </StackPanel>
        <Border Grid.Row="1" Background="Transparent"/>
        <Grid x:Name="MatrixGrid" Grid.Row="2" Margin="10"/>
    </Grid>
</Window>
